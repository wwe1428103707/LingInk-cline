title = "Cline SDK secret scanning"

[extend]
useDefault = true

[allowlist]
paths = [
	# Academic Research Skills (bundled and source) contain synthetic citation
	# keys (e.g. "vaswani2017-arxiv-v1", "Test2023ArXiv") in test fixtures,
	# eval gold sets, design docs, and examples. gitleaks flags these as
	# generic API keys, but they are third-party fixtures under CC-BY-NC-4.0
	# and are not secrets.
	'''apps/vscode/bundled-skills/lingink-ars/''',
	'''plugins/academic-research-skills/''',
]
