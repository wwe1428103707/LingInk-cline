/**
 * Constants used across the chat view components
 */
export const CHAT_CONSTANTS = {
	MAX_IMAGES_AND_FILES_PER_MESSAGE: 20,
	/** Show the Quick Wins panel only while the user has fewer completed tasks than this. */
	QUICK_WINS_HISTORY_THRESHOLD: 3,
} as const
